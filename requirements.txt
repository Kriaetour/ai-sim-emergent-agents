# AI Sandbox (Refactored) -- dependencies
#
# The simulation itself uses only Python standard library modules.
# Ollama must be running separately (https://ollama.com) when MYTHOLOGY_ENABLED = True.

# ── Core simulation ────────────────────────────────────────────────────────
noise==1.2.2              # Perlin noise for coherent terrain + sea depth generation

# ── Live dashboard (dashboard.py + dashboard_bridge.py) ────────────────────
streamlit                 # web dashboard framework
plotly                    # world map heatmap + reputation line chart
numpy                     # biome RGB array construction for imshow
streamlit-autorefresh     # 2-FPS polling without manual refresh

